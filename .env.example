# Copy to .env (which is gitignored) and fill in your key.
# Server reads this on startup. Never exposed to browser.

OPENAI_API_KEY=sk-...your-key-here...
OPENAI_MODEL=gpt-4o-mini

# Optional: override port (defaults to 5173).
# PORT=3000
